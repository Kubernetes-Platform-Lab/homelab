apiVersion: v1
kind: Pod
metadata:
  name: debugpod
  namespace: kube-system
spec:
  hostPID: true
  containers:
    - name: debugcontainer
      image: alpine:3.20
      stdin: true
      tty: true
      securityContext:
        privileged: true
      volumeMounts:
        - name: dev-mount
          mountPath: /dev
        - name: host-mount
          mountPath: /host
  volumes:
    - name: dev-mount
      hostPath:
        path: /dev
    - name: host-mount
      hostPath:
        path: /
  nodeSelector:
    kubernetes.io/hostname: cd-node02
