apiVersion: v1
kind: ConfigMap
metadata:
  name: cert-manager-values
  namespace: cert-manager
data:
  values.yaml: |
    installCRDs: true

    prometheus:
      enabled: false

    webhook:
      timeoutSeconds: 30

    replicaCount: 1
